import { ShadowOnHoverDirective } from './shadow-on-hover.directive';

describe('ShadowOnHoverDirective', () => {
  // it('should create an instance', inject([ElementRef], (el: ElementRef) => {
  //   const directive = new ShadowOnHoverDirective(el);
  //   expect(directive).toBeTruthy();
  // }));
});
